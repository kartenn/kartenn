query get($projectUuid: ID) {
    getProject(projectUuid: $projectUuid) {
        projectUuid
        name
        urlRepository
        idRepository
        codeOwners
        languages
        slackRoom
        devAlias
        hasReadMe
        createdTs
        updatedTs
        diskUsage
        type
    }
}
